<%= form_for(@digital_object) do |f| %>
  <% if @digital_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@digital_object.errors.count, "error") %> prohibited this digital_object from being saved:</h2>

      <ul>
      <% @digital_object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
     <%= f.label :digital_object_id %><br>
     <%= f.text_field :digital_object_id %>
   </div>

    <div class="field">
      <%= f.label :home_repository %><br>
      <%= f.text_field :home_repository %>
    </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
